import math

n = 1000
lst = [True for i in range(n + 1)]

for i in range(2, int(math.sqrt(n)) + 1):
    if lst[i] == True:
        j = 2

        while i * j <= n:
            lst[i * j] = False
            j += 1
